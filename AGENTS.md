# AGENTS.md

## 项目定位

这是一个中文个人网站 / 个人博客，不是 SaaS、简历站或内容平台。

网站的核心气质是私人、安静、清新、克制。功能设计应该服务于表达、记录和整理，而不是追求复杂系统感。

## 语言约定

面向用户的交流、需求文档、规格文档、实现计划、README 以中文为主。

代码、文件名、schema 字段、环境变量、commit message 使用英文。

不要在没有明确理由的情况下把中文需求文档写成英文。

## 当前技术方向

前台使用 Astro。

公开内容后台使用独立 Sanity Studio。主站不提供 `/admin` 页面。

内容页应动态读取 Sanity 中已发布内容，而不是依赖每次重新构建前台。

## 协作方式

开始实现前，先阅读最新的 `docs/superpowers/specs` 和 `docs/superpowers/plans`。

不要把未确认的未来方向提前做进当前版本。尤其是登录、真实 AI、RAG、真实访客簿、私密内容库。

UI 修改应延续现有极简、温和、内容优先的风格。

## 内容安全

不要提交 `.env` 或任何密钥。

不要把真实人格说明书、ChatGPT 历史、近期状态摘要、私人笔记、私密 RAG 材料提交到公开仓库。

如果不确定某类内容是否适合公开，默认不要提交。
